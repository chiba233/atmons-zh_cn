# 本文件列出 All the Mons 1.2.0 在**全新实例**上 options.txt 里 resourcePacks 那一行
# 的内容，一行一项、顺序照抄。安装器靠它决定汉化包插在第几位。
#
# 注释行与空行会被 build_dist.sh 剔掉，所以下面一项都没有 = 「尚未实测」。
# 此时安装器**不伪造** resourcePacks 行，改走两步流程：第一次只写 lang:zh_cn，
# 提示玩家先启动一次游戏、退出后再运行一次安装器——那一次会把汉化包挪到列表最后
# 一位，必定正确。伪造一个不全的列表反而会让汉化包被压在内置包底下、还没有任何提示。
#
# ── 实测来源 ──────────────────────────────────────────────────────────────
#
# 1.2.0 实例的 options.txt（28789 字节、589 行）里 resourcePacks 那一行，逐项照抄。
#
# 取值时看的是**写全了的那份**。同一个文件早先读到过 `resourcePacks:[]`，那是游戏
# 还没把这一行写出来的中间态：文件当时的 mtime 停在启动后两分钟，进过世界、干净
# 退出之后才写成现在这样。**空列表不能采信**——判据是文件写全没有，不是文件在不在。
#
# 两个 zip 资源包（`RCT Trainers+ [1.6] v2.1.zip`、`Maxi's-JM+CM_Pixel-U11.zip`）
# 确实在生效（日志 ReloadableResourceManager 的活动包列表里有），但**不在下面这份
# 列表里**，这是对的：整合包自带的 config/global_packs.toml 写着
#
#     [resourcepacks]
#     required = ["resourcepacks/", "global_packs/required_resources/"]
#
# globalpacks 把整个 resourcepacks/ 目录当必需资源包强制启用，必需包不进玩家的
# 选择列表、也就不写进 options.txt。所以「列表里没有它们」不等于「漏了」。
# （顺带：这条 required 也会强制启用我们的汉化包 zip。）
#
# 别按 `globalpacks` 去 grep 配置——文件名是 `global_packs.toml`；也别拿空的
# `global_packs/` 目录当「这模组没干活」的判据，那只是 required_resources 的挂载点。
modularbees:dynamic_assets
vanilla
mod_resources
moonlight:merged_pack
mod/dyenamicsandfriends:compat_packs/productivemetalworks/
mod/dyenamicsandfriends:compat_packs/connectedglass/
mod/dyenamicsandfriends:compat_packs/luminax/
mod/dyenamicsandfriends:compat_packs/cookingforblockheads/
mod/dyenamicsandfriends:compat_packs/botanypots/
mod/dyenamicsandfriends:compat_packs/chromacarvings/
