# 从 atm10-zh-cn 迁移的进度

本文件只记**还没做完的部分**与做每一项时已经测得的数字，做完一项就删掉一项。
全部做完后删除本文件。

判据一律对着 All the Mons 1.2.0 的真实字节算：模组是否存在按 jar 内
`assets/<命名空间>` 判，不按 jar 文件名判（`silent-gear-*.jar` 的文件名带连字符，
按名字前缀查会误判成缺失）。

## 已完成

- 构建管线整套（`scripts/` `.github/` `installer/` `src/rules/`），包身份只钉在
  `paths.py` 的 `PACK_NAME` 与 `fetch_pack.py` 的 `PROJECT`
- `src/pack` 259 个命名空间 / 95953 条；`src/books` 822 个文件；
  `src/vaultpatcher` 159 个模块（全量，不在搬家时替构建做取舍）；`src/kubejs` 4 个文件
- `versions/1.2.0/` 与 `versions/db/1.2.0/` 基线（CI 生成、核对后入库）

## 待办

### 一、任务书译文

All the Mons **不带 zh_cn**（只有 en/es/fr/ja/ko/pl/pt/ru/tr/uk 十种），
All the Mods 10 带。两边任务 id 与英文原文有大量逐字节相同的部分。

已测（对着两边的 `quests/lang` 逐键比对，解析器先用「回写必须逐字节相同」自检过
305 个文件）：

| | 条数 | 占 All the Mons |
|---|---|---|
| 键在 + 英文原文逐字节一致 | 7291 | 78.2% |
| 键在但英文改过 | 143 | 1.5% |
| 无中文可搬 | 1821 | 19.7% |

那 143 条按相似度分：≥0.99 有 77 条（多为 All the Mons 引入的拼写差异，
如 `it's`/`redstone controll`/`aquired`），<0.70 有 25 条是真改了内容。

要做的：写**仓库脚本**按两份基线生成，不许在 scratchpad 里跑一次性代码往 `src/` 灌
文件。硬规矩：**英文原文不逐字节一致的条目一律报出来，不许静默搬**——id 相同而原文
被改写过时套用旧译文，比漏翻更糟。

1821 条从零翻的集中在新章节：`legendaries` 443、`allthemodium_cobblemon` 124、
`enderio` 114、`paradox` 97、`getting_started` 92、`productive_farming` 92。

### 二、`src/upstream` 重新推导

**不能搬，要重推。** 已测 332 条定点映射在 All the Mons 上的命中情况：

| | 文件数 |
|---|---|
| 全部命中 | 13 |
| 部分失配 | 2 |
| 全部失配 | 38 |
| 目标文件不存在 | 24 |

逐条 edit 只有 31/332（9.3%）仍唯一命中。目标文件不存在的那批说明 All the Mons
换掉了整块内容：`kubejs/startup_scripts/Modern-Industrialization/**`、
`kubejs/server_scripts/mods/modular_machinery/multiblocks/**` 13 个文件、
`kubejs/assets/atm10_localization/lang/zh_cn.json`（All the Mods 10 靠它注入物品名）。

用 `extract_upstream_patch.py` 对着 All the Mons 的官方文件重推；13 个全命中的留下。

### 三、`versions/1.2.0/default_resource_packs.txt`

**必须实测**：整合包首次启动后游戏自己写出的 `resourcePacks` 顺序。安装器靠它决定
汉化包插在第几位，写错会让汉化包被压在内置包底下、等于没启用。取法是启动一次到
主菜单后读 `options.txt` 的 `resourcePacks:` 那一行。

### 四、`src/config`

任务书 delta 落在这里。等第一项做完一并进来。

## 上一个仓库里的同一处缺陷

`build_version_db.py` 的类搬家回退搜索按匿名内部类的序号建索引，本仓库已修
（见 `fix: 类搬家回退搜索不再按匿名内部类的序号建索引`）。同一处在 atm10-zh-cn
里仍在，只是那边目标类都存在、触发不到。是否回移未定。
