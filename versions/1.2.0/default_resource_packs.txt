# 本文件列出 All the Mons 1.2.0 在**全新实例**上 options.txt 里 resourcePacks 那一行
# 的内容，一行一项、顺序照抄。安装器靠它决定汉化包插在第几位。
#
# 注释行与空行会被 build_dist.sh 剔掉，所以下面一项都没有 = 「尚未实测」。
# 此时安装器**不伪造** resourcePacks 行，改走两步流程：第一次只写 lang:zh_cn，
# 提示玩家先启动一次游戏、退出后再运行一次安装器——那一次会把汉化包挪到列表最后
# 一位，必定正确。伪造一个不全的列表反而会让汉化包被压在内置包底下、还没有任何提示。
#
# ── 已经量到的事实（1.2.0 实例，2026-08-14 那次运行）────────────────────────
#
# 那次运行是完整的：09:52 启动、进过世界、10:04:44 走完关闭流程干净退出。
# 但 options.txt 的 mtime 停在 09:54:27，内容是 resourcePacks:[]，退出时没有再写。
# 这个 [] **不能采信**，因为同一次运行里有两个 zip 资源包确实在生效
# （日志的 ReloadableResourceManager 那行里有 `RCT Trainers+ [1.6] v2.1.zip`
# 与 `Maxi's-JM+CM_Pixel-U11.zip`，两个文件也确实在 resourcepacks/ 下）。
# 一个空列表配两个生效的包，说明这次拿到的不是那一行的最终值。
#
# 排除过的可能：config/defaultoptions/options.txt 只有 44 字节、没有 resourcePacks
# 行，所以不是 Default Options 在旁路管理这一项。
#
# 取值办法：起一次全新实例，**停在主菜单**，从游戏内的「选项 → 资源包」里确认列表，
# 再正常退出，然后读 options.txt 的 resourcePacks: 那一行，按顺序逐项抄到下面。
