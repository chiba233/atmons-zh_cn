# All the Mons 1.2.0 上，各类生成物的**实测张数/份数**。
#
# 这些数字不是拍出来的下限，是拿本版整合包跑一遍生成器量出来的基线。
# build_dist.sh 用它做「不许回退」的判据：低于基线就红。
#
# 为什么不再写死一个数：原先的 1300 / 200 是**上一个整合包**的量级。
# 两个包的模组集合不同，同一套映射能落地的份数本来就不一样，
# 拿旧包的数字卡新包，红的是「模组换了」，不是「汉化少了」——
# 那种红没有信息量，只会逼人去调数字，闸就废了。
#
# 换整合包版本时要重新量一次，并且**必须在这里留下改动记录**，
# 这样任何一次下调都在 git 里看得见，而不是悄悄改个魔数。
#
# ── 导览书（patchouli_books / ae2guide / oracle-index 的输出文件数）──────
#
# 实测 1294。同一次运行里 gen_books.py 自己报的是：
#     结构型 812 个文件、3633/3633 条译文落位（100.0%）
#     与原文相同没写 0 个；该版本没有的跳过 713 个
# 也就是说译文这一侧是满的，一条没漏；1294 这个数是「本版有哪些模组」
# 决定的，跟翻译质量无关。那 713 份是上一个整合包有、这一版没有的模组的书。
books = 1294
#
# ── 横幅（章节标题图，assets/atm/textures/questpics）────────────────────
#
# 实测 169。原先是 162：那是从上一个整合包的映射里摘掉「本版没有的模组」
# （iceandfire 22、mahou 12、iron_spells 1、xycraft 1、building_tips 2）之后的数。
# 摘对了，但**没往回加**——All the Mons 自己新增的宝可梦章节的标题图从来没进过
# 那张表，于是侧边栏是中文、章节顶上的艺术字还是英文。2026-08-15 补了 7 张章节级的
# （新的开端、全都找出来！、大师球、超级对决、与宝可梦一起种田、高效+农业），
# 又补了 12 张章节内的（进化、悖谬、宝可技巧与窍门、究极+异兽、考古、钓鱼、
# ATM 团队系列、作物、自动农场、花卉、变异）。
# 仍未补的是章节内部的小标题图，见 gen_quest_banners.py 顶部。
banners = 199
#
# ── 主菜单按钮（config/fancymenu/assets）───────────────────────────────
#
# 14 张，与上一个整合包相同，没有变化。
buttons = 14
#
#
# ══ 打包之后的实测量 ═══════════════════════════════════════════════════
#
# 上面三项是「生成器产出了多少」，下面这组是「zip 里最后有多少」，
# 由 verify_dist.py 用（下限 = 实测 × 0.95，向下取整，它自己算，不写死）。
#
# 每一项后面注了来源。[CI] = CI 的 Build 实测打印，全部十项都是。
#
# verify_dist.py 无论过没过都会把实测的 got 打出来，所以任何一项记错了，
# 下一次 CI 会当场报出真值——那时改这里的基线，不许改余量。
# client_buttons 就是这么抓出来的：一开始按「生成器报了 14 张按钮」填 14，
# CI 实测 20，差在这一项数的是整个 fancymenu/assets 目录，不只是按钮。
#
client_lang_files  =    262   # [CI] assets/*/lang/zh_cn.json
client_lang_keys   = 103920   # [CI] 上面那些文件里的键总数
client_banners     =    199   # [CI] 资源包里那份，与生成器侧 banners=199 互相印证
client_kubejs_banners = 199   # [源] kubejs/assets 里那份，与上面同源同数；
                              #      游戏里真正生效的是这一份（KubeJS 压过资源包）
client_buttons     =     20   # [CI] 注意**不是**上面的 buttons=14：那是生成器出的
                              #      按钮张数，这里数的是包里 config/fancymenu/assets/
                              #      下的全部 png，实测 20 个
client_vp_modules  =    156   # [CI]
client_quest_delta =     82   # [CI]
client_gui_files   =     24   # [CI]
client_book_files  =   1097   # [CI] patchouli_books / ae2guide / oracle-index 的文件数
#
server_vp_modules  =     10   # [CI]
server_quest_delta =     82   # [CI]
